Stores credentials only in the `0600` credentials file, and drops the opt-in OS keychain store.
`$BOOKSHELF_USE_KEYCHAIN` no longer does anything, and `keyring` is no longer a dependency.
Run `bookshelf auth login` again if an earlier run left a secret in the keychain.
