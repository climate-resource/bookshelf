Removes the producer facade pass-through beneath `Bookshelf` and `AsyncBookshelf`.
The calls behave exactly as they did.
`visibility=` on `draft_book` now accepts the same input as every other registration surface.
