Replaces `liccheck` with `licensecheck` for the dependency licence check.
`liccheck` has had no release since September 2023.
`licensecheck` reads `pyproject.toml` directly,
so `make licence-check` no longer has to export the dependencies to a temporary requirements file.
