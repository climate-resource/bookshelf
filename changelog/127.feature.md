Added REST API client, WorkOS OAuth authentication, and backend abstraction layer (`BookshelfBackend` protocol with `S3Backend` and `APIBackend` implementations) as the foundation for migrating from direct S3 access to API-driven data fetching.
