Collapses the four hand-rolled copies of the token exchange onto the credential providers.
`bookshelf auth token` now asks the provider for a token rather than deciding staleness itself,
so a stored login carrying a refresh token but no recorded expiry is refreshed before it is printed,
where it used to be printed as it stood.
The stored credential format is unchanged.
