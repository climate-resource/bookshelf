Limits the API reference to the stable reading surface of the SDK.
Only what it documents is covered by Semantic Versioning, so the producer methods, `bookshelf.publisher`, `bookshelf.models` and the CLI can still change in any release.
