Added `Bookshelf.ensure_authenticated()` and its async twin, which confirm the API accepts the ambient credential.
A missing or spent stored login now starts a browser login from a terminal, or a device code login from a notebook.
CI client credentials and `BOOKSHELF_TOKEN` are verified but never replaced.
Anywhere else it raises the new `AuthenticationRequiredError`.
Drafting a book runs the same check first.
