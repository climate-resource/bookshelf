Moved `data_dictionary=` from book drafting and updating to `DraftBook.attach()`
and `AsyncDraftBook.attach()`.
Each book entry now carries its own dictionary.
Omitting the argument preserves an existing entry dictionary on re-attach,
while an empty list clears it.

Refreshed the vendored OpenAPI contract and generated models,
removing the retired book-level fields and exposing dictionaries on entry responses.
