Changed the default API URL to the production deployment, `https://bookshelf.climateresource.com.au`.
Pass `base_url=` or set `$BOOKSHELF_URL` to keep using staging.
Stored logins are scoped per deployment, so a staging login does not carry over.
Run `bookshelf auth login` again to authenticate against production.
