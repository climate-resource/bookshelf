Added support for storing arbitrary pandas DataFrames as Book resources via `LocalBook.add_dataframe()` and `LocalBook.dataframe()` methods. DataFrames are stored in Parquet format with optional gzip compression.
