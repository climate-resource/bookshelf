Moved `data_dictionary=` from book drafting and updating to `DraftBook.attach()`
and `AsyncDraftBook.attach()`.
Each book entry gained its own dictionary.
Omitting the argument preserved an existing entry dictionary on re-attach,
while an empty list cleared it.

Entry responses exposed the dictionary that applied to that entry.
